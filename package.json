{
  "name": "planify",
  "version": "1.0.0",
  "description": "Planificador de perfis metálicos dobrados (DXF/PDF/SVG) — front em HTML único servido por Node.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch public --ext html,js,css --exec node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
